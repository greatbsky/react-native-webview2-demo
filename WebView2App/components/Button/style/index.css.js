'use strict';

import {
  StyleSheet
} from 'react-native';

export default StyleSheet.create({
    button: {
        borderRadius: 3,
        alignItems: 'center',
        justifyContent: 'center',
        padding: 3,
        backgroundColor: '#cccccc',
        margin: 9
    },
    text: {
        textAlign: 'center',
    },
});
